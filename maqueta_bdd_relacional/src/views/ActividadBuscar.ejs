<%- include("template/headTemplate") %>
<%- include("template/navbar1Template") %>

<div class="container col-md-8">

  <h2>ACTIVIDADES</h2>
  <h1>BUSQUEDA DE ACTIVIDAD</h1>

  <a href="/">Inicio</a> -> <a href="/Formulario/ActividadBuscar">Buscar</a>

    <form action="/Formulario/Colaboradores" method="POST">

    <div class="row p-2">
    <div class="form-group col-md-10">
      <input class="form-control" type="text" name="actividadBuscar" id="id_actividadBuscar"></input>
    </div>
    <div class="form-group col-md-2">
      <button class="btn btn-primary btn-block">BUSCAR</button>
    </div>
  </div>
    </form>

    <table class="table">
      <thead>
        <tr>
          <th scope="col">Número</th>
          <th scope="col">Nombre</th>
          <th scope="col">Fecha</th>
          <th scope="col">Responsable</th>
        </tr>
      </thead>
      <tbody>
         
        <% if (array_vcm_actividades.length > 0) { %>
            <% array_vcm_actividades.forEach(vcm_actividades => { %>
                <tr>
                    <th scope="row">
                      <a href="/Formulario/ActividadInformacion/<%= vcm_actividades.id_actividad %>"><%= vcm_actividades.id_actividad %></a>
                    </th>
                    <td>
                      <%= vcm_actividades.nombre %>
                    </td>
                    <td>
                      <%= vcm_actividades.fecha %>
                    </td>
                    <td>
                      <%= vcm_actividades.id_academico %>
                    </td>
                  </tr>
            <% }) %>
        <% } %>
        </tbody>
    </table>

</div>

</body>

</html>
